Sample : CommonsLoggingTestWebapp
===================

Introduction
============

This is simple webapp sample to generate logs using Apache Commons API

This uses Apache Commons API and log4j as logging back end. The generated logs will be routed to default log4j appenders
as specified in log4j.properties file bundled with the web app WEB-INF/classes directory.
This webapp will be deployed and the http GET method will be invoked by the WebAppLoggingTestCase.